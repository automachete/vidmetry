# Vidmetry

<img src="src-tauri/icons/128x128.png" width="80" height="80" alt="Vidmetry">

Vidmetryは、動画の画面領域と再生区間を、プレビューで確認しながら切り取って保存するWindows 11向けデスクトップアプリです。画面上の枠で残したい領域を決め、必要であればフレーム列から開始位置と終了位置を調整します。動画編集ソフトのプロジェクトやタイムラインを用意する必要はなく、1本の動画にもフォルダー内の複数の動画にも、同じ手順で使えます。

Windows標準フォトの動画トリミングでは扱えない画面領域の切り取りにも対応し、保存時は再生互換性、ファイル容量、画質のどれを優先するかに合わせて保存方法を選べます。よく使う形式や画質は共通設定に保持されるので、普段の操作は動画を開き、範囲を決め、保存先を選ぶだけです。

## Vidmetryでできること

- プレビュー上の枠をドラッグして、動画の上下左右を切り取る
- 座標と大きさをピクセル単位で指定し、縦横比も固定できる
- 動画の開始位置と終了位置をフレーム単位で調整する
- 再生、ループ再生、スクロール操作で保存前に内容を確認する
- 動画またはフォルダーを、エクスプローラーやドラッグ＆ドロップから開く
- フォルダー内の動画を一覧またはPage Up／Page Downで切り替える
- 元動画を残す「コピーして保存」と、確認付きの上書き保存を使い分ける

動画の読み込みから保存まではPC内で完結し、動画を外部サーバーへアップロードしません。表示にはWindowsのライト／ダークモードとアクセントカラーが反映され、表示言語は日本語と英語から選べます。

## 画質と保存方法

Vidmetryでは、再生しやすさ、ファイル容量、画質のどれを優先するかに応じて、三つの保存方法を選べます。普段の共有に使いやすいMP4、画素を保つ可逆保存、圧縮済みの映像をそのまま維持するメタデータのみの保存には、それぞれ次のような長所と短所があります。

| 保存方法 | メリット | デメリット |
|---|---|---|
| 互換MP4 | 多くの端末やプレイヤーで扱いやすく、画質、ファイルサイズ、互換性のバランスを詳細に調整できます。 | 映像を再エンコードするため、設定によっては追加の圧縮による画質劣化が生じます。 |
| 可逆保存 | 画面領域や再生時間を実際に切り取ったうえで、デコード後の画素を劣化させずに保存できます。 | ファイルサイズが大きくなり、再生できるソフトも限られます。 |
| メタデータのみ | 対応するH.264／HEVC動画を再エンコードせず、元の圧縮済み映像をそのまま維持できます。 | クロップ情報を解釈しないプレイヤーでは切り取りが反映されず、時間トリミングとも併用できません。 |

互換MP4では、H.264／H.265、CRF、エンコード速度、画素形式、フレームレートのほか、音声のコピーまたは変換、音声ビットレート、字幕とメタデータの保持、Web再生向けの最適化を設定できます。保存方法と詳細設定は共通設定に保持されるため、動画を開くたびに指定し直す必要はありません。出力形式が元動画と同じ場合は、元動画を残すか上書きするかを保存時に選べます。

## 使い方

1. 動画またはフォルダーをVidmetryへドロップするか、選択ボタンから開きます。
2. プレビュー上の枠を動かして、画面に残す領域を決めます。
3. 必要であれば、画面下のフレーム列で開始位置と終了位置を調整します。
4. 再生またはスクロール操作で内容を確認し、「コピーして保存」または「保存」を選びます。

## 主なショートカット

| キー | 操作 |
|---|---|
| `Space` | 再生／一時停止 |
| `←` / `→` | 選択中の位置を1フレーム調整 |
| `Shift` + `←` / `→` | 開始／終了位置を10フレーム調整 |
| `Page Up` / `Page Down` | フォルダー内の前／次の動画へ移動 |
| `Ctrl` + `S` | コピーして保存 |
| `Ctrl` + `Shift` + `S` | 元動画へ保存 |
| `F11` / `Esc` | 全画面プレビューの開始／終了 |

## インストール

対応環境はWindows 11 x64です。[GitHub Releases](https://github.com/automachete/vidmetry/releases)から最新のセットアップEXEまたはMSIをダウンロードしてインストールしてください。

インストールすると、対応動画の「プログラムから開く」とフォルダーの右クリックメニューからVidmetryを起動できます。この連携は共通設定で無効にできます。

## ライセンスと開発

Vidmetry本体は[MIT License](LICENSE)です。同梱するFFmpeg／ffprobeなど第三者ソフトウェアのライセンスと対応ソースについては[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)を確認してください。

開発環境の構築やコントリビューションについては[CONTRIBUTING.md](CONTRIBUTING.md)、詳細な設計は[docs/SDD.md](docs/SDD.md)にまとめています。
